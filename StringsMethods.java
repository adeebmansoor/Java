// import java.util.Scanner;
public class StringsMethods {
    public static void main(String[] args) {
        String s1 = "HELLO WORLD";
        // String s2 = "Hello World";
        // String s3 = "hello World";
        // 1. length() :- Used to check the length of the String.
        // System.out.println(s1.length());

        // 2. toUpperCase() :- Used to convert the String into Upper case.
        // System.out.println(s1.toUpperCase());

        // 3. toLowerCase :- Used to convert the String into Lower case.
        System.out.println(s1.toLowerCase());

        // 4. equals :- Used to check if both the String are same or not.
        // System.out.println(s1.equals(s2));
        // System.out.println(s1.equals(s3));

        // 5. equalsIgnoreCase :- Used to check if both the String are same or not, while ignoring the cases.
        // System.out.println(s1.equalsIgnoreCase(s2));

        // 6. compareTo() :- Used to check the values of both strings.
        // System.out.println(s1.compareTo(s3));
        // System.out.println(s1.compareTo(s2));

        // 7. charAt() :- Used to returns particular character at particular index.
        // System.out.println(s1.charAt(6));
        // System.out.println(s1.charAt(9));

        // 8. toCharArray() :- Used to convert the String into an array.
        // String str = "Java";
        // char [] arr = str.toCharArray();
        // for-each loop :- It used for representing an array, this cannot performs any calculations and other activities.
        // Syntax :- for( datatype variable_name: array_name){
        //  Statement;
        // }
        // for(char ch : arr){
        //     System.out.println(ch);
        // }

        // 9. next() :- Used to take input a String, it only returns one word.
        // Scanner sc = new Scanner(System.in);
        // String str2 = sc.next();
        // System.out.println(str2);

        // 10. nextLine() :- Used to take input a String, it can return more than one word.
        // String str3 = sc.nextLine();
        // System.out.println(str3);

        // 11. next().charAt() :- Used to take Input as character.
        // char ch = sc.next().charAt(4);
        // System.out.println(ch);
        // sc.close();

        // // 12. replace() :- Used to replace a word from a particular String.
        // String s4 = "Bhopal, is city of pools.";
        // System.out.println(s4.replace("pools", "lakes"));
    }
}
